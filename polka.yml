---
- name: Setup Polka
  hosts:
    - localhost
  connection: local
  gather_facts: false
  vars:
    username: tmiller
    go_version: 1.6.2
    home_dir: "/Users/{{ username }}"
    homebrew_packages:
      - coreutils
      - ctags
      - curl
      - jq
      - openssl
      - readline
      - tree
      - watch
    git:
      name: Tom Miller
      email: tmiller@eab.com

  roles:
    - package
    - bash
    - tmux
    - git
    - go
    - ack
    - vim
    - ruby
    - python
    - ansible
